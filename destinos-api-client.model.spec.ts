import { DestinosApiClient } from './destinos-api-client.model';

describe('DestinosApiClient', () => {
  it('should create an instance', () => {
    expect(new DestinosApiClient()).toBeTruthy();
  });
});
